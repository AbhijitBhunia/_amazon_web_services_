EC2 instance and security group creating using terraform without hardcoading the AMI ID and instance type.

Commands:
terraform init
terraform validate
terraform plan
terraform apply -auto-approve
terraform plan -destroy
terraform destroy -auto-approve